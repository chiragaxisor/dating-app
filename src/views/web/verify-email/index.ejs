<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <title>Verify Email | <%= process.env.APP_NAME || "Loosh App" %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link
      rel="shortcut icon"
      type="image/x-icon"
      href="<%= process.env.APP_URL  %>/images/favicon.ico"
    />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@4.4.1/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&family=Playfair:wght@400;500;600&display=swap"
      rel="stylesheet"
    />
    <style>
      body,
      html {
        font-family: 'Inter', sans-serif;
      }
      a {
        color: #dd6b63;
      }
      .container h1 {
        font-weight: bold;
        color: #dd6b63;
      }
      .display {
        font-family: 'Playfair', serif;
        font-weight: 600;
      }
    </style>
  </head>

  <body>
    <div class="container">
      <div
        class="d-flex align-items-center justify-content-center flex-column text-center vh-100"
      >
        <img
          src="<%= process.env.APP_URL  %>/images/full-logo.png"
          alt="logo"
          width="100px"
        />
        <div class="container mt-5">
          <% if(user){ %>
          <h1 class="h1 display">Welcome to the community!</h1>
          <br />
          <h5 class="h5 mt-3">
            You can now close the window and proceed to login in the app.
          </h5>
          <% }else{ %>
          <h1 class="h1 display">Sorry! This link has expired.</h1>
          <br />
          <h5 class="h5 mt-3">Please try to registering again.</h5>
          <% } %>
        </div>
      </div>
    </div>
  </body>
</html>
