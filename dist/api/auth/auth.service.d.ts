import { AccessTokensService } from '../access-tokens/access-tokens.service';
import { RefreshTokensService } from '../refresh-tokens/refresh-tokens.service';
import { Users } from '../users/entities/user.entity';
import { LoginDto } from './dto/login.dto';
import { RegisterDto } from './dto/register.dto';
import { UsersService } from '../users/users.service';
import { ForgotPasswordDto } from './dto/forgot-password.dto';
import { MailerService } from '@nestjs-modules/mailer';
import { ConfigService } from '@nestjs/config';
import { ResetPasswordDto } from './dto/reset-password.dto';
import { VerifyResetPasswordOtpDto } from './dto/verify-reset-password-otp.dto';
import { Repository } from 'typeorm';
import { DeviceTokens } from '../device-tokens/entities/device-tokens.entity';
import { LogoutDto } from './dto/logout.dto';
export declare class AuthService {
    private usersService;
    private accessTokensService;
    private refreshTokensService;
    private mailerService;
    private configService;
    private readonly userRepository;
    private deviceTokenRepository;
    constructor(usersService: UsersService, accessTokensService: AccessTokensService, refreshTokensService: RefreshTokensService, mailerService: MailerService, configService: ConfigService, userRepository: Repository<Users>, deviceTokenRepository: Repository<DeviceTokens>);
    register(registerDto: RegisterDto): Promise<{
        authentication: {
            accessToken: string;
            refreshToken: string;
            expiresAt: any;
        };
        jti?: string;
        id: number;
        userUniqueId: string;
        name: string;
        email: string;
        password: string;
        address: string;
        country: string;
        city: string;
        state: string;
        gender: import("../../common/constants").Gender;
        coins: number;
        isSubscribed: boolean;
        subscriptionExpiry: Date;
        profilePic: string;
        isNotificationOn: boolean;
        isOnline: boolean;
        forgotPasswordCode: string;
        forgotPasswordCodeExpiredAt: Date;
        isBlocked: boolean;
        createdAt: Date;
        updatedAt: Date;
        deletedAt: Date;
        dateOfBirth: Date;
        imlooking: string;
        relationship: string;
        about: string;
        interested: string;
    }>;
    login(loginDto: LoginDto): Promise<{
        authentication: {
            accessToken: string;
            refreshToken: string;
            expiresAt: any;
        };
        jti?: string;
        id: number;
        userUniqueId: string;
        name: string;
        email: string;
        password: string;
        address: string;
        country: string;
        city: string;
        state: string;
        gender: import("../../common/constants").Gender;
        coins: number;
        isSubscribed: boolean;
        subscriptionExpiry: Date;
        profilePic: string;
        isNotificationOn: boolean;
        isOnline: boolean;
        forgotPasswordCode: string;
        forgotPasswordCodeExpiredAt: Date;
        isBlocked: boolean;
        createdAt: Date;
        updatedAt: Date;
        deletedAt: Date;
        dateOfBirth: Date;
        imlooking: string;
        relationship: string;
        about: string;
        interested: string;
    }>;
    generateTokens(user: Users): Promise<{
        accessToken: string;
        refreshToken: string;
        expiresAt: any;
    }>;
    forgetPassword(forgotPasswordDto: ForgotPasswordDto): Promise<void>;
    verifyResetPasswordOtp(verifyResetPasswordOtpDto: VerifyResetPasswordOtpDto): Promise<void>;
    resetPassword(resetPasswordDto: ResetPasswordDto): Promise<void>;
    logout(user: Users, logoutDto: LogoutDto): Promise<void>;
}
